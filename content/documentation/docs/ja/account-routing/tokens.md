API キーは Aivrae へのリクエストを認証します。アプリ、環境、顧客ごとに別キーを使うと、制限、ローテーション、無効化を個別に行えます。

- production キーを frontend コード、GitHub、公開ログ、スクリーンショットに載せないでください。
- テスト環境には低上限の別キーを使います。
- 漏えいした場合は直ちに無効化し、新しいキーを作成します。
- サポートにはキー名または末尾のみ伝えます。
